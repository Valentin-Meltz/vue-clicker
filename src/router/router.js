/*
import { createRouter, createWebHistory } from 'vue-router';
import Basics from '../components/basics/main.vue';
import RouterPage from '../components/router/main.vue';
import Store from '../components/store/main.vue';
import Clicker from '../components/clicker/main.vue';
import Home from '../App.vue';

const routes = [
  { path: '/', component: Home },
  { path: '/basics', component: Basics },
  { path: '/router', component: RouterPage },
  { path: '/store', component: Store },
  { path: '/clicker', component: Clicker },
];

const router = createRouter({
  history: createWebHistory(),
  routes
});

export default router;
*/